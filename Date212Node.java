public class Date212Node {
	protected Date212 Data; 
	protected Date212Node Next; 
	//This class will store the Date in data and the pointer to the next node for a list.  
	public Date212Node(Date212 d) { // Used to create a new node with the input d as it's data to store. 
		Data = d;
		Next = null;
	}
}
